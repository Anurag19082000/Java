public class OneDay {
    public static void main(String[] args) {
        System.out.println("Hello, Kanha!");
    }
}   


// This program prints a greeting message to the console.// It serves as a simple demonstration of a Java program structure.// The main method is the entry point of the program where execution begins.

// now to upload this file to github repository.

/*  Run git init to initialize the project,
 then add your GitHub repository using git remote add origin <repo-url>,
  stage all files with git add ., commit them using git commit -m "Initial project upload",
   set the main branch using git branch -M main,
    and finally upload everything to GitHub with git push -u origin main.



    After editing a file, run git add . to stage the changes, then use git commit -m "Updated file" to commit them, and finally run git push to upload the updated changes to GitHub.
    
    */

